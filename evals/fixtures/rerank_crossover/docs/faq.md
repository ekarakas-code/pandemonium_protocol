# Billing FAQ

How is the billing amount computed? The monthly billing amount is computed from metered usage,
the plan unit rate, and the discount. Where is the billing amount computed, and how is the
overdue late fee computed? Both the billing amount and the overdue late fee are computed in the
billing service layer, and this FAQ explains how each computed charge is derived.
