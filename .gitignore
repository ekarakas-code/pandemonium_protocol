# --- Python build / cache artifacts ---
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/

# --- Virtual environments ---
.venv/
venv/
env/
ENV/

# --- Test / type-check caches ---
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- Pandemonium index data store (large, machine-specific; rebuilt by the tool) ---
# Matches the live store and any nested fixture/stray indexes at every level.
.pandemonium/

# #11 eval matrix: regenerated per run by `run_eval.py --matrix` (the durable lock is
# evals/matrix_baseline.json, which IS tracked); the latest run artifact is not.
evals/fixtures/matrix/matrix_result.json

# Local external-repo A/B gold (machine-specific abs paths + a private repo's internals;
# the harness loads it via QA_TASKS — kept out of the tree on purpose).
evals/qa_tasks_igya.json

# --- OS cruft ---
.DS_Store
Thumbs.db
